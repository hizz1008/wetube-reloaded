extends ../base

block content
  if errorMessage
    span= errorMessage
  form(method="post")
    input(placeholder="기존 비밀번호", type="password", name="oldPassword")
    input(placeholder="변경 비밀번호", type="password", name="newPassword")
    input(placeholder="변경 비밀번호 확인", type="password", name="newPassword1")
    input(value="적용", type="submit")
